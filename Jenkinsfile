pipeline {
    agent any

    stages {
        stage('creating virtual env') {
            steps {
                script{r=bat(run_wrapper.bat)
                }
                
            }
        }
        
    }
}
