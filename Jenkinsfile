pipeline {
    agent any

    stages {
        stage('creating virtual env') {
            steps {
                script{r=bat( script : '''run.run_wrapper.bat
                    ''')
                }
                
            }
        }
        
    }
}
