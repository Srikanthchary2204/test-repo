pipeline {
    agent any

    stages {
        stage('creating virtual env') {
            steps {
                script{cmd run_wrapper.bat}
                }
                
            }
        }
        
    }
}
