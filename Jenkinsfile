pipeline {
    agent any

    stages {
        stage('creating virtual env') {
            steps {
                script{r=bat( script : bat run_wrapper.bat
                    )
                }
                
            }
        }
        
    }
}
